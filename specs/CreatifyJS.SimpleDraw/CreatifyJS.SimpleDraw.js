describe('CreatifyJS.SimpleDraw', function () {
	var canvas = document.getElementsByTagName('canvas')[0];
	var draw = null;
	describe('Drawing a line', function () {
		draw = new CreatifyJS.SimpleDraw(canvas);
	});
});